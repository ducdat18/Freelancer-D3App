The screenshots referenced in Section~\ref{section:4.5} (Figures~\ref{fig:demo_create_job}--\ref{fig:demo_dao_vote}) accompany the thesis defence package. A short demonstration video walks through an end-to-end engagement -- create job, submit bid, fund escrow, submit work, release or dispute, mint reputation SBT, KYC, and DAO vote -- and links each on-chain action to its Solana Explorer signature page. The Devnet transaction signatures of Table~\ref{table:tx_samples} link to \emph{https://explorer.solana.com/tx/\textless{}sig\textgreater{}?cluster=devnet}; the Anchor program at Program ID \emph{FStwCj8z\ldots yyP7i} is publicly inspectable via the same explorer.

The observability stack described in Section~\ref{section:4.7} (\emph{monitoring/} in the repository) is also reproducible: a single \emph{docker compose up} command boots the Prometheus exporter, Prometheus, and Grafana with a pre-provisioned dashboard, against the same Devnet deployment.
