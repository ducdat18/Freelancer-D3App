global:
  scrape_interval: 30s
  evaluation_interval: 30s
  external_labels:
    project: freelancechain
    cluster: devnet

scrape_configs:
  - job_name: fc-exporter
    static_configs:
      - targets: [exporter:9464]
    metrics_path: /metrics
